# learned-review 学習台帳

運用ルールは SKILL.md「学習の更新」。**実際にこのプロジェクトで起きたパターンのみ**を載せる（一般論は書かない）。

## L-001 部分的 i18n ハードコード（接尾辞・接頭辞・区切り）
- 初出: 2026-07 以前 `${displayName}号室` の接尾辞だけハードコードのままコミットし、ユーザー指摘で発覚
- パターン: 大半が catalog 経由でも、テンプレートリテラルの接尾辞/接頭辞/区切り文字だけ生の日本語が残る
- 検査: `grep -nP "[\p{Hiragana}\p{Katakana}\p{Han}]" <編集ファイル>` をコメント行除外で確認
- status: active

## L-002 冪等判定が「存在」だけ見て「同一性」を見ない
- 初出: 2026-07-15 `PairPage.tsx`（`hasIdentity` のみで冪等スルーし、QR の DID とローカル DID を照合せず。別 ID を作成済みの端末が永久に紐づけ不能になった）
- パターン: 「既に有るならスキップ」の冪等分岐で、「有るもの」が期待対象と同一かの照合が抜ける
- 検査: 冪等/スキップ/早期 return 分岐を見たら「何と何の同一性をもって冪等なのか」を diff に問う
- status: active

## L-003 エラー時の誘導先が別の不可逆操作を誘発する
- 初出: 2026-07-15 `PairPage.tsx`（ペアリング失敗→「オンボーディングへ」誘導→独立グループ創設、という事故導線）
- パターン: 失敗時のフォールバック CTA が、失敗原因の解消（再試行・再発行）ではなく別状態の新規作成になっている
- 検査: エラーパスの CTA を「リトライ/原因解消」か「新規作成系」かに分類し、後者なら生成物の独立性・巻き戻し可否を確認
- status: active

## L-004 状態変更後の再配線漏れ（起動時にしか読まれない値）
- 初出: 2026-07-15 `PairPage.tsx`（completePairing 後にリロードせず、bootstrap 時 seed のままの LinkSelf 配線が残った）
- パターン: bootstrap/初期化時にしか読まれない値（seed・networkId・名前空間ポインタ等）を実行中に書き換えて、再読込/再配線をしない
- 検査: localStorage 等への書き込みを見たら「その値はいつ・どこで読まれるか」を確認。起動時のみなら reload/再配線の有無を確認
- status: active

## L-006 対をなす別ストレージの破棄漏れ（localStorage だけ消して OPFS 等が残る）
- 初出: 2026-07-15 `group-slots.ts` purge（localStorage の名前空間キーのみ破棄し、対の OPFS `hvs-group-<slotId>.db` が無期限残存。独立レビューが commit 前に検出）
- 再発: 2026-07-16 AI 地図取込の全面廃止（コードは全削除したが、廃止前に保存済みの旧 AI 設定＝**平文 API キー**が localStorage / LinkSelf `my_settings` に無期限残存し、削除 UI も同時に消えて本人が消せなくなる構図。独立レビューが commit 前に検出）。**機能廃止 diff では「コード削除」と対の「保存済みデータの破棄（起動時一度きり掃除等）」が要る**
- パターン: 1 つの論理データが複数ストレージ（localStorage + OPFS/IndexedDB 等）に分かれているとき、破棄処理が片方だけを消す。機能廃止時はコード削除だけでデータ破棄が抜ける
- 検査: 破棄/削除系・機能廃止系の diff では、対象の論理データが載る全ストレージ（CODEMAP・ファイル名導出関数の使用箇所）を列挙して突合。即時削除不能なら孤児記録等の代替があるか確認
- status: active

## L-007 複数所属化の過渡期に残る単一ポインタへの無条件上書き
- 初出: 2026-07-15 `linkself-services.ts` hookedIdStore.set（追加参加で既存スロットの networkId を上書きし別グループのデータが混在し得た。独立レビューが commit 前に検出）
- パターン: 「1 つだけ」前提だった書き込み経路（アクティブポインタ・単一キー）が、複数対応の仕様化後もガード無しで残る
- 検査: 複数所属/複数実体を導入する diff では、旧単一前提のポインタへの set/上書き箇所を grep し、既存値と異なる値が来た場合の分岐が仕様どおりか確認
- status: active

## L-008 新規オーバーレイがコンテンツ内の既存浮遊 UI に z-index で負ける
- 初出: 2026-07-15 `style.css` サイドバーオーバーレイ（z:1000 で追加したが、`.tip-stack` z:9999・`.visit-page-list-button`/`.drawing-toolbar` z:1000 等がコンテンツ側に既存し、同値後勝ち/高値で前面に残った。独立レビューが commit 前に検出）
- パターン: モーダル的なオーバーレイ/backdrop を追加するとき、リポジトリ内の既存 z-index の分布を確認せず低い値を割り当てる
- 検査: オーバーレイ追加の diff では `grep -n "z-index" pwa/src/style.css | sort -t: -k3 -n` で既存最大値を確認し、モーダル層はそれより上に置く。backdrop より上に残ってよい UI か個別に判断
- status: active

## L-009 サイズ上限のある出力経路に可変長データを載せて無言破損
- 初出: 2026-07-15 `QrCode.tsx` × ペアリング payload v2（メンバー数比例で伸びる payload が QR 容量上限 2331B を超えると console.error のみで空白 canvas。独立レビューが commit 前に検出）
- パターン: 出力先（QR・URL 長・プッシュ通知本文等）に固定上限があるのに、載せるデータを可変長に拡張し、超過時のユーザー可視のフォールバックが無い
- 検査: payload/出力データを拡張する diff では、出力経路の上限（QR 容量・URL 実務上限等）と最大想定サイズを見積もり、超過時の可視挙動を確認
- status: active

## L-010 OPFS SAHPool はプールディレクトリ単位で排他（複数 DB 同時 open が衝突）
- 初出: 2026-07-15 増分①の DB 分割（個人 DB+グループ DB の 2 ファイル同時 open で 2 つ目の installOpfsSAHPoolVfs が NoModificationAllowedError → グループ DB が in-memory フォールバックし users が永続されず。**レビュー 2 回とも見逃し・本番でユーザー報告により発覚**）
- パターン: OPFS SAHPool VFS はプールディレクトリ内の全ファイルの Access Handle を排他取得するため、既定プールを共有する複数 worker の同時 open は必ず失敗する。かつ失敗が catch でフォールバックされると**無言でインメモリ化**し永続性だけが消える
- 検査: `SqliteWasmDatabase.open`（または SAHPool install）の呼び出しが 1 実行パスに複数ないか。複数あれば vfsPool（プール分離）指定を確認。catch フォールバックがあるなら「失敗がユーザー/ログからみえるか」も確認
- status: active

## L-011 「自分除外」フィルタが同一アカウントの兄弟端末まで落とす
- 初出: 2026-07-15 link-self groupshare（メンバー配送・catch-up 要求・要求受理・アカウント解決のすべてが「自アカウント除外」で、同一 DID の別端末が配送対象に一度も入らず、ペア済み端末へグループデータが流れなかった。独立レビューが commit 前に検出）
- パターン: 2層 identity（アカウント＋デバイス）で「self を除外する」ルーチンがアカウント単位で書かれ、自アカウントの他デバイスという正当な宛先まで除外される
- 検査: メンバー/宛先列挙・membership 判定の diff では「自分除外」の粒度（アカウントかデバイスか）を問い、兄弟端末が到達可能かをトレース
- status: active

## L-012 後で特権処理される「控えキュー」が主体（ユーザー）に紐づいていない
- 初出: 2026-07-15 `hvs.pendingSiblingDevices`（起動時にユーザー鍵でロスターへ追加署名される控えが、どのユーザー宛かを持たず・切替時の purge 対象にも無く、別ユーザーのデバイスが新ユーザーのロスターに署名混入し得た。独立レビューが commit 前に検出）
- パターン: 「後で信頼付与される待ち行列」（pending/queue/控え）が、付与先の主体と紐づかずに永続され、主体切替後に消費される
- 検査: pending/queue 系キーの diff では (a) エントリが主体（ユーザー DID 等）を持つか (b) 消費側が主体照合するか (c) 主体切替の purge 経路に含まれるか、を確認
- status: active

## L-013 バージョン順序 merge（高い方全面採用）導入時に旧・無バージョンデータの移行が無い
- 初出: 2026-07-15 ロスター rev 化（デプロイ済み旧版の v1 ロスターが unmarshal で rev 0 になり、新規ペア端末の rev 2 ロスターに「高 rev 全面採用」で潰されて既存の兄弟端末が脱落し得た。独立レビューが commit 前に検出）
- パターン: rev/epoch で「高い方が全面勝ち」の merge を導入するとき、既存永続データがバージョン欠落（=0 扱い）のまま残ると、内容の薄い新規ピアの低いが正のバージョンに richer な旧状態が丸ごと潰される
- 検査: 単調 rev・LWW 化する diff では「デプロイ済みの無バージョンデータが load 時に何として扱われるか」「新規作成物の初期値がそれを超えないか」「load 経路に一度きりの移行（再署名/再スタンプ）があるか」を突合
- status: active

## L-014 否定条件（リストからの不在）を失効・削除と解釈する検知の誤爆
- 初出: 2026-07-15 端末削除（「ロスターに自分が居ない＝削除された→全初期化」の判定が、ペアリング直後の正当な過渡状態＝発行側の高 rev ロスターがまだ新端末を学んでいない、で確定誤爆し新端末が即ワイプされる経路。独立レビューが commit 前に検出。tombstone=明示的失効記録の導入で修正）
- 再発: 2026-07-16 地図同期（saveAll=全体スナップショット保存 API を行単位同期ストアに接続した際、「現在行にあるがスナップショットに無い行=DELETE」が、loadAll 後に ScopeNetwork で届いた他端末の新規行を削除し墓石を全員へ伝播し得た。独立レビューが commit 前に検出。削除対象を「直近 loadAll でエディタへ提供した行」に限定して修正）
- パターン: 「リストに無ければ削除とみなす」型の検知は、未収束・ブートストラップ過渡の正当な不在と意図的な削除を区別できない。破壊的アクション（ワイプ・購読解除・BAN・同期削除の墓石発行）のトリガーには明示的な否定記録（tombstone）か「知っていた集合」への限定が要る
- 検査: 「〜が無ければ削除/失効とみなす」分岐を見たら、(a) その不在がブートストラップ/マージ過渡で正当に発生しないか (b) 明示シグナル（tombstone 等）に置き換えられないか (c) union 系 merge が削除を復活させないか (d) 全量スナップショット保存 API を行単位ストアに接続する diff では、スナップショット取得後に受信した行が削除対象に入らないか、を問う
- status: active

## L-005 主体切替時のローカル状態の部分的な持ち越し
- 初出: 2026-07-15 `PairPage.tsx` の別 DID 切替（旧 ID の networkId・グループ名・自己レコードが新 ID 側に残り得る問題を実装時に対処）
- パターン: identity・アクティブグループ等の「主体」を切り替える処理で、主体に紐づくローカルキーの一部だけ破棄し残りが持ち越される
- 検査: 切替系 diff では対象主体に紐づく全ローカルキーを列挙（CODEMAP・`grep -rn "localStorage" 該当層`）して破棄/保持の判断を突合
- status: active

## L-015 Leaflet ベクタ層の重なりを一回きりの bringToFront/bringToBack で固定
- 初出: 2026-07-16 `map-renderer.ts` 親番境界線（追加時のみ bringToFront したが、renderAll・差分再描画がポリゴン層を remove→再 add すると境界線が輪郭の下に沈む。独立レビューが commit 前に検出。専用ペイン=createPane+zIndex 固定で修正）
- 再発: 2026-07-16 `map-renderer.ts` 場所オーバーレイ（bringToFront すら無くデフォルト overlayPane に追加。頂点ドラッグ中の applyChangeSet がポリゴンを remove→再 add するたび灰色アイコンが塗りの下に沈む。独立レビューが commit 前に検出。専用ペイン zIndex 450 で修正）。**bringToFront の有無に関わらず「デフォルトペインへの追加＋別 layer の再 add 経路」自体がこのパターン**
- パターン: Leaflet の同一ペイン内 z 順は DOM 追加順のため、bringToFront/bringToBack は「その時点」の順序しか保証しない。後から同ペインへ layer が再 add される経路（全再描画・modified 再構築）で順序が崩れる
- 検査: 新しい layer 追加や bringToFront/bringToBack を含む diff では、同ペインへ layer が後から add される経路を列挙し、順序が再確立されるか・専用ペイン（createPane + zIndex）で代替できるかを問う
- status: active

## L-016 @media 上書きを同一セレクタのベース規則よりソース順で前に書いて無効化
- 初出: 2026-07-16 `style.css` `.checkouts-list-panel`（狭幅上書きの @media ブロックをベース規則より前に挿入し、同一詳細度の後勝ちでベースが常に適用＝上書きが死んでいた。独立レビューが commit 前に検出）
- パターン: メディアクエリは詳細度を上げないため、同一セレクタの上書きはベース規則より後ろに無いと効かない。追記位置が既存ベース定義より前だと無言で無効化される
- 検査: @media 追加の diff では、上書き対象セレクタのベース定義行番号と @media ブロックの行番号を `grep -n` で比較し、media が後にあることを確認
- status: active

## L-017 fit 系 API へのローカル maxZoom 上限が小さい対象のフィットを破る
- 初出: 2026-07-16 `map-renderer.ts` focusPolygons（flyToBounds に maxZoom:17 を指定。150m 四方の区域のタイトフィットに必要な z19 がキャップされ、訪問記録画面「地図を区域へ戻す」で区域が画面の一部にしか映らず周辺の広い範囲が表示された。実機でユーザー体感バグとして発覚＝レビュー見逃し）
- パターン: fitBounds/flyToBounds/getBoundsZoom 系にオプションで固定 maxZoom を渡すと、対象 bounds が小さいときフィットズームがキャップされ「対象が画面に収まるビュー」にならない。ズーム上限は map 全体の maxZoom 設定に委ねるのが既定
- 検査: fitBounds/flyToBounds を含む diff で maxZoom オプションの有無を見る。指定があれば「最小サイズの対象で必要なフィットズーム」が上限を超えないか、map レベルの maxZoom で代替できないかを問う
- status: active

## L-018 同一データへの規則（表示換算・自動修復）が箇所ごとに別実装で食い違う/競合する
- 初出: 2026-07-16 場所オーバーレイ追加時に 2 件同時検出（独立レビューが commit 前に検出）: (1) SortOrder 全件 0 の未初期化区域を「重複」と誤判定し、既存の CreatedAt 昇順初期採番（訪問記録画面）と幾何順再採番（区域編集画面）が競合＝先に開いた画面が勝つ状態になっていた。(2) 番号バッジの換算が画面ごとに別実装（訪問記録画面の地図=位置 index で欠番を詰める / 一覧パネルと新オーバーレイ=SortOrder+1）で、欠番のある区域では同じ場所が画面によって別番号表示になっていた
- パターン: 同じフィールド（番号・状態）を読む表示換算や、同じデータを書く自動修復規則が複数箇所に別実装されると、前提条件の重なり（未初期化 vs 重複）や換算差（詰める vs 詰めない）で挙動が食い違う。どちらが発動するかが「どの画面を先に開いたか」に依存する競合は特に気づきにくい
- 検査: 番号・状態の表示や自動修復を追加する diff では、同じフィールドを読む/書く既存箇所を grep（例: `sortOrder`）して列挙し、(a) 換算規則が単一実装（共有関数）か、(b) 自動修復規則が複数あるなら前提条件が排他的かを確認する
- status: active

## L-019 セッション/モード中の確定入力が stopPropagation 付きレイヤーハンドラに横取りされる
- 初出: 2026-07-16 場所移動（右クリック→移動→確定クリックで訪問ダイアログが開き確定不能。**ユーザー報告で発覚＝当時のレビュー見逃し**）。確定ハンドラを map の click に置いたが、追従中のマーカー自身の click（stopPropagation＋ダイアログ起動）が必ず先に受けていた。さらに修正レビューで、タッチではマーカーが追従しない（mousemove が無い）ため確定タップがポリゴン click（stopPropagation 付き）に落ちる残存経路も検出
- パターン: 「次のクリックで確定/選択」する一時モードの確定ハンドラを map（最下層）に置くと、その上の interactive レイヤー（マーカー・ポリゴン等）の stopPropagation 付きハンドラが入力を横取りし、モードの確定が発火しない。カーソル追従要素は確定クリックが必ず自要素上に落ちる。マウスとタッチで落ちるレイヤーが異なる（追従の有無）ことにも注意
- 検査: 「次のクリック/タップで確定」型のモードを導入・変更する diff では、クリックが落ち得る interactive レイヤーを全列挙（`grep -n "stopPropagation" 該当 renderer`）し、各ハンドラがモード中ガード（単一の確定実装への委譲）を持つか確認。マウス/タッチ両方の当たり先を考える。半径 10px 以下の circleMarker では e.latlng がマーカー座標へ差し替えられる点（originalEvent から取り直す）も確認
- status: active

## L-020 受信再読込の画面カバレッジが仕様文言と食い違う
- 初出: 2026-07-16 全グループドメインテーブルの ScopeNetwork 移行（wants に「開いている画面（…訪問記録）が再読込する」と書いたが、訪問記録画面は places/visit_records の受信購読が無く、出入りし直すまで反映されなかった。独立レビューが commit 前に検出）
- パターン: 同期受信で画面を再読込する仕組み（イベント購読）を追加する変更では、仕様に列挙した画面と、その画面が表示する全データ種（一覧・併記・マーカー・バッジ）に対する購読テーブルの対応が漏れやすい。特にコンテナ/子ページ分割時は、子が内部 refreshKey を持つと外からの更新経路が無いことに気づきにくい
- 検査: 受信購読を足す diff では、仕様（wants）に書いた「再読込する画面」一覧 × 各画面の表示データ種を列挙し、購読テーブル群（shared-events のプリセット）と突合する
- status: active

## L-021 描画のみの擬似バックドロップ（box-shadow 100vmax 等）は遮断も階層も持たない
- 初出: 2026-07-16 訪問記録ダイアログ群（box-shadow: 0 0 0 100vmax の暗転はポインタイベントを遮断せず、ダイアログ表示中に背後の地図がスワイプ可能だった。かつ実要素でないため z 階層も本体任せで、狭幅サイドバーレール（z 10001）の下に潜った。ユーザー報告で発覚。実要素 .dialog-backdrop（inset:0, z 10500）で修正）
- パターン: 暗転を box-shadow / 擬似要素で「描くだけ」のモーダルは、(a) 背面のポインタ操作を遮断できず、(b) バックドロップ自身のスタッキング制御ができない。モーダル化には inset:0 の実要素バックドロップが要る
- 検査: モーダル風 UI の diff で box-shadow 100vmax・全面 ::before 等の擬似暗転を見たら、背面要素へのポインタ到達（地図ドラッグ・スクロール等）と z 階層（最前面レイヤーとの照合）の両方を問う
- status: active

## L-022 wants 改訂が重複記述のある兄弟仕様ファイルへ波及しない
- 初出: 2026-07-16 編集リクエスト詳細テキスト任意化（08 の 2 節は改訂したが、07「申請種別」の「3種別いずれも詳細テキスト必須」と申請種別テーブルが旧ルールのまま矛盾残存。独立レビューが commit 前に検出）
- 再発: 2026-07-16 申請一覧本実装（07/10 の該当節は改訂したが、10 冒頭の実装状況注記「/requests はスタブ」・09 積み残し「申請タスクへの反映は次期スコープ」・concept.md 論点-004 が旧記述のまま矛盾残存。独立レビューが commit 前に検出）
- 再々発: 2026-07-16 申請対象への遷移実装（07/10 の該当節は改訂したが、同じ 10 の §11 実装順序表 E 行「未着手・RequestsPage スタブ」と NotificationBinding 行「次スコープ」が残存。**同一ファイル内でも節が離れると漏れる**。独立レビューが commit 前に検出）
- パターン: 同じ運用ルール・実装状況が複数の wants テーマ（例: 07 申請種別 × 08 ダイアログ挙動、10 冒頭注記 × 状態表）や concept.md に重複記述されていると、仕様改訂 diff が起点ファイルだけを直し、参照先/兄弟の明文が矛盾したまま残る
- 検査: wants 改訂 diff では、変更した規則のキーワード（例: 「詳細テキスト」「必須」）を docs/wants/ 全体で grep し、同じ規則を述べる他ファイルの行を列挙して突合する。機能を「実装済み」化する diff では、対象機能名とともに「スタブ」「未実装」「未着手」「次期スコープ」「次スコープ」を docs/ 全体（concept.md・**変更したファイル自身の別節（冒頭注記だけでなく表・脚注）**含む）で grep し、虚偽化した記述を同一コミットで直す
- status: active
## L-023 ID 指定の対象フォーカス機能が階層下位・不可視の実体を無言で指し損ねる
- 初出: 2026-07-16 申請一覧→訪問記録への遷移（?place=<placeId> で対象場所を選択状態で開く機能が、部屋（room）を対象とする申請では効かなかった。部屋はマーカー・場所一覧行を持たない（表示対象集合は parentId なしの場所のみ）ため、selectedPlaceId がどこにもマッチせず無言で無選択になる。独立レビューが commit 前に検出。遷移元で親の集合住宅 ID へ読み替えて修正）
- パターン: ID 参照で対象を指し示す機能（クエリパラメータ・ディープリンク等）は、その ID が階層下位の実体（部屋）や論理削除済み実体を指すとき、受け取る側の表示機構（マーカー・一覧行）に存在せず無言で効かない
- 検査: ID 指定フォーカス機能の diff では、渡し得る ID の全種別（発行箇所を遡って列挙）と受け側の表示対象集合を突合し、「表示対象外の ID が来たときのフォールバック（親への解決 or 可視フィードバック）」を問う
- status: active
