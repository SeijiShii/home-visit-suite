# 継続的検討事項

## 全面 PWA 化（2026-07-07 決定）に伴う未決事項

決定済み: 単一 PWA + ロール別 UI へ全面転換、Wails 版は新規開発凍結・段階的廃止、LinkSelf は TS 実装（link-self リポジトリ側で開発）。

- **PWA の実装場所（2026-07-07 決定）**: 本リポジトリに `pwa/` を新設する（別リポジトリにはしない）
  - 旧 `desktop/frontend` の React 資産・i18n catalog・style.css をコピー移植し、`docs/wants/`（SoT）および参照実装としての `shared/`（Go）と同居させる。移植完了後、`desktop/` は削除済み（2026-07-07）
- **Go 資産（`shared/`）の移植方式（2026-07-07 決定）**: TS で手書き再実装する（Go→WASM 流用はしない）
  - `docs/wants/` を SoT として `pwa/` 内の domain/service 層を TS で書き直す。Go 実装（テスト込み）は移植時の参照実装として使う
  - 理由: LinkSelf TS 実装・sqlite-wasm と同一言語で自然に結合でき、バンドルも小さく、長期的に TS 一本化できる
- **着手タイミング（2026-07-07 決定）**: LinkSelf TS 実装の完成を待たず `pwa/` に先行着手する
  - データ層をリポジトリ・インターフェースで分離（Go 側の domain.Repository パターンと同型）し、インメモリ実装で画面・ドメイン層の移植を先行。LinkSelf TS 完成時に永続化アダプタを差し替える

以下は未決。

- **常時稼働ノードの初期構成**: ブラウザピアに必要なリレー／ブートストラップ／メールボックスをどこに置くか
  - 候補: Oracle Cloud Always Free（恒久無料のフル VM、推奨候補）／運用者のローカル Windows 機（回線が MAP-E / DS-Lite だとポート開放不可の罠あり）／併用による二重化
  - 設計上は複数ノードを登録できる形にする（`11_LinkSelf拡張要望.md` §3.1.3）ため、後から追加・移行可能。まずローカルで PoC → 本運用でクラウド追加が有力
- **Wails → PWA 移行の段階計画**: 画面・ドメイン・サービスの UI/ロジック移植は完了し、`desktop/`（Wails 版）は削除済み（下記「PWA 移行の進捗」参照）。残るは LinkSelf TS 永続化アダプタへの差し替えのみ（現状はインメモリ + localStorage 暫定）
- **Go 資産の TS 移植順序**: ドメインモデル・サービス層（CheckoutService の可用性バリデーション、ForceCloseExpiredCheckouts 等）は移植済み。DB マイグレーション相当（sqlite-wasm スキーマ）は LinkSelf TS アダプタ実装時に対応する

### PWA 移行の進捗（2026-07-07）

`pwa/`（Vite + React + TS + vite-plugin-pwa）へ以下を移植済み。データ永続化のみ LinkSelf TS 完成までインメモリ + localStorage の暫定実装。

- **ドメイン層**: `shared/domain` の全モデル・全リポジトリインターフェースを TS 化
- **サービス層**: Auth / AvailablePeriod / Checkout を TS 再実装（Go 参照実装の権限・排他・段階ロック等を再現、時刻は `nowFn` 注入）
- **全 9 画面**: Dashboard / Users / Requests / Checkouts / Coverage / RegionManagement / Map（ポリゴン編集、map-polygon-editor + leaflet）/ AreaDetailEdit / Visit。Wails Binding は `useServices()` のリポジトリ・アダプタに置換
- **リポジトリ・アダプタ境界**: 旧 Wails Binding 相当を `RegionRepositoryBindingAdapter` / `PlaceRepositoryBindingAdapter` / `VisitBindingAdapter` / `PersonalRepositorySettingsAdapter` / `LocalStorageMapBinding` として実装。LinkSelf TS 完成時は `createInMemoryServices`（`pwa/src/contexts/ServicesContext.tsx`）の実装差し替えで移行できる
- **起動時 reconcile**: `reconcileOnStartup` が `forceCloseExpiredCheckouts` を起動時に実行
- **テスト**: vitest でドメイン・サービス・各画面をカバー（119 件）
- **Phase 1 暫定の解消**: VisitPage は固定区域 `NRT-001-01` を廃し `/visits/:areaId` の areaId を対象化。訪問記録の本保存経路（RecordVisit）はアクセスモードに応じて選択
- **バックグラウンドジョブの設計転換（方式決定・PWA 実装済み）**: PWA はタブが閉じていればコードが動かず、常時稼働ノードは E2E 暗号化の「土管」でありアプリロジックを実行できない。期限系処理は「アプリ起動時 reconcile」方式とし、`pwa/src/contexts/ServicesContext.tsx` の `reconcileOnStartup` が起動のたびに `forceCloseExpiredCheckouts` を実行する。長時間開きっぱなしのタブへの対応（フォアグラウンド復帰時の再実行等）は画面移植後に検討
- **鍵・ローカルデータの永続化と喪失リスク**: DID 秘密鍵と sqlite-wasm データはブラウザストレージに載るため、ストレージ削除＝端末喪失と同等になる。機種変更・ブラウザ変更時の鍵移行 UX、他ピアからの再同期による回復可否を link-self 側仕様（`browser-pwa-support.md`）と合わせて確定する（`11_LinkSelf拡張要望.md` への追記候補）
- **通知の期待値調整**: `07_通知と申請.md` の通知は Web Push 代理ノード（`11_LinkSelf拡張要望.md` §3.3.1、将来スコープ）なしではアプリ未起動時に届かない。当面「起動時に未読を取得」で足りるか、通知仕様側の改訂要否を整理する
- **オフライン戦略**: 訪問活動は屋外利用が前提。App shell の Service Worker キャッシュに加え、GSI タイルのオフラインキャッシュをどこまでやるか（GSI タイルの利用規約確認を含む）
- **モバイルレイアウト**: 移植した画面はサイドバー前提のデスクトップレイアウト。ロール別 UI と合わせて `10_画面設計.md` を PWA（モバイルファースト）前提に改訂する
- **iOS PWA の案内 UX**: ホーム画面追加の誘導（Safari の共有メニュー経由でやや分かりにくい）、Web Push が iOS 16.4+ かつホーム画面追加時のみ有効である旨の案内
- **PWA 配信ホスティング**: 静的配信のみなので選択肢は広い（Vercel 等で可。常時稼働ノードとは分離する。`link-self/docs/spec/browser-pwa-support.md` 参照）

## 権限（解決済み）

- 編集メンバーが稼働できないときに活動メンバーだけで区域を取得できるように限定的な権限範囲の追加が必要か
  - → 解決。「貸し出し／持ち出し」の操作経路区別を廃止し、活動メンバーも編集メンバー以上も同じチェックアウト操作で区域を取得できる
  - 他者のアクティブなチェックアウトが無い区域であれば、活動メンバーが自分自身を担当者として直接発行可能
  - 詳細は「05_チェックアウト.md — 区域の取得（チェックアウト）モデル」を参照

## メンバーグループ廃止（解決済み）

- App 内のメンバーグループ（OrgGroup）概念は 2026-05-06 に廃止
  - メンバー分類はロール（管理者・編集メンバー・活動メンバー）とメンバータグのみで表現する
  - 「Aチーム／Bチーム」のような運用上の分類が必要な場合はメンバータグで運用する
  - 旧 `OrgGroup`、`User.groupId`、`UserBinding.SaveGroup/DeleteGroup/ListGroups`、招待ダイアログの「OrgGroup フィルタ」、SchedulePeriod のグループスコープなどは全廃止

## SchedulePeriod / AvailablePeriod 廃止（解決済み）

- 2026-05-06 に「SchedulePeriod / Scope / AreaAvailability（貸し出し可能・持ち出し可能の階層）」を廃止し、`AvailablePeriod`（チェックアウト可能期間）に統合
  - 承認フローは廃止し、監査ログによる事後追跡に置換
- 2026-07-13 に `AvailablePeriod`（チェックアウト可能期間）自体も廃止
  - チェックアウトのゲートは「他者のアクティブなチェックアウトが無ければ可」の排他制約のみに簡素化
  - 網羅進捗の期間スコープ・期間終了による強制クローズも撤廃
  - 「いつ・どの範囲を活動対象にするか」を定義する期間オブジェクトは将来再導入する可能性がある（旧仕様は git 履歴から復元可能）
  - 詳細は `05_チェックアウト.md` / `06_網羅管理.md` を参照

## 招待された活動メンバーの再訪問ニーズ（ペンディング）

- 区域招待（`05_チェックアウト.md`）で招待された活動メンバーが、当該区域で活動中に「後日改めて訪問したい場所」を発見するケースの扱いは未決定
- 必要性
  - 招待の有効期限切れ後にも、被招待者が当該場所の情報を**参照**したり、**個人記録（個人メモ・個人タグ等）を追加**したりしたい場面がある
  - 例: 「会えなかったが在宅予定の時間帯を聞いたので後日寄ってみたい」「外国語対応で再訪が必要」など、招待単位の活動で完結しない継続的フォロー
- 想定される論点
  - 期限切れ後にどの情報をどこまで参照可能にするか（場所情報のみ／自分の過去訪問記録のみ／全訪問記録、等）
  - 期限切れ後に追加できるのは個人スコープ（`PersonalNote` / 個人タグ）のみとするか、ネットワーク共有の訪問記録追加まで許すか
  - 個人記録のみ許可する場合、その入口 UI（区域からはアクセスできない前提で、場所単位の個人ノート画面など）
  - 招待延長／再招待ワークフローと、担当者・編集メンバーへの再訪要望の通知
  - 担当者本人による再訪問との重複・衝突の調整
- **設計の流用予定**: 期限切れ後の特定場所アクセスは `05_チェックアウト.md` の「場所レベル read-only モード」（場所単位のアクセスモデル）を流用して実現する想定。場所レベルのモデルは現フェーズで先行整備済み（既定 editable）、UI からの切替操作は本ペンディングと併せて設計する。
- 本フェーズではスコープ外。チェックアウトモデル本実装後に再検討する

## 区域系画面の権限切り分け

- 区域編集（`/map`）など区域系画面の利用可能ロールを別途整理する
- 場所編集は 2026-07-14 に訪問記録画面へ統合し、権限×端末の切り分けを確定した（直接編集＝編集メンバー以上×非タッチ端末。`07_通知と申請.md`「場所操作の権限」）
- 訪問記録画面（`08_活動メンバー向けアプリ.md`）のアクセス条件も確定済み（活動＝自分のチェックアウト or 有効な招待／編集以上＝常時）

## 場所も座標もない訪問記録の扱い

- 旧仕様で「場所も座標も持たない訪問記録」を活動メンバーの個人データとして許容していたが、本フェーズではスコープ外とした
- 必要性（GPS取得失敗時の救済、純粋な個人メモ用途等）と、訪問記録メニューからの起動経路を別途設計する

## 場所作成申請の「その他」種別の定義

- 場所作成申請ダイアログの種別選択肢「その他」（家・集合住宅以外）の正式定義を別途検討
- 想定される対象: 店舗、公共施設、事業所、寺社、学校など
- 「その他」をさらに細分化するか、自由記述に委ねるかも論点

## ロングタップによる場所作成申請の誤操作防止 UX

- 訪問記録画面では原則ロングタップを使わない方針に対して、場所作成申請のみ例外的にロングタップで起動する
- モバイルでの誤操作（地図操作中の意図しないロングタップ）を防ぐ UX を別途検討
- 候補: 確認ダイアログを必ず挟む、長押し時間を通常より長く設定、起動前にトースト/ヘルプを出す等

## 訪問記録画面の Phase 1 暫定実装の解消

進捗（2026-05-04 時点、Phase G0〜G7 完了後）:

- ✅ `actorId`（自分の DID）の取得経路を `IdentityContext` で配線済み（Phase G0）
- ✅ チェックアウトモデルおよび区域招待を実装済み（Phase A〜F）。アクセスモード判定 API も提供（`AreaAccessMode` / `PlaceAccessMode`）
- ✅ `useAreaDetailMap` フックで地図共通化済み
- 残課題:
  - 訪問記録の保存はまだ `RecordVisitPhase1`（チェックアウト不要）を経由している。活動メンバーの記録は本実装の `RecordVisit(actorID, checkoutID, ...)` への切替が必要（当該区域の active checkout から `checkoutID` を解決）。編集メンバー以上の直接記録は CheckoutID なしのままでよい（2026-07-14 改訂で編集メンバーはチェックアウト不要と確定。`08_活動メンバー向けアプリ.md`「区域」）。
  - 「場所作成申請」「場所情報修正申請」の永続化は `console.log` のみ。`RequestService` 経由の保存と申請タスクへの反映は次期スコープ。
  - （旧「招待を依頼」CTA は 2026-07-14 の区域レベル read-only 廃止で UI ごと撤去済み。招待依頼の通知ワークフローが必要になったら別途設計）

## 訪問活動概念廃止に伴うコード反映（解消済み）

仕様書は 2026-05-04 に「訪問活動 (Activity) / 訪問活動チーム (Team) / ActivityTeamAssignment」を廃止し、`05_チェックアウト.md` に集約済み。実装も Phase A〜G7（コミット `9b1d1a1`〜`e9c28b8`）で全面追従済み。

完了した内容:

- Go モデル: `Team` / `ActivityTeamAssignment` 削除、`Activity` → `Checkout`、`VisitRecord.ActivityID` → `CheckoutID`
- リポジトリ: `ActivityRepository` → `CheckoutRepository`、ファイルリネーム、Team/Assignment メソッド削除
- サービス: `ActivityService` → `CheckoutService`、`AssignTeam` 削除、`ReassignOwner`（後に `ReassignPersonInCharge` へリネーム予定） / `Invite` / `RevokeInvite` / `ListInvitations` / `AreaAccessMode` / `PlaceAccessMode` / `ListAccessibleAreas` / `ListAllCheckouts` を追加
- DB スキーマ: V1 を原型に戻し V7 マイグレーションで `activities` → `checkouts`、`activity_id` → `checkout_id`、`teams` / `activity_assignments` 削除、`checkout_invitations` 新設
- 区域招待: `CheckoutInvitation` モデル + `checkout_invitations` テーブル、Service 層で発行・取消・連動失効
- Wails Binding: `CheckoutBinding` 新設、`IdentityBinding` 新設（dev モード切替）
- フロントエンド: サイドバー整理（ロール別ガード）、ダッシュボード「アクセス可能な区域」「全ての区域一覧」、`CheckoutsPage` 本実装、`InviteDialog`、訪問記録画面の二層 read-only モード

2026-05-06 の仕様改訂により、可用性モデルは大きく変更:

- 旧: スコープ／貸し出し可能／持ち出し可能の階層 + 編集メンバーバイパス
- 新: AvailablePeriod の対象区域親番リストによる単純制約（編集メンバーバイパスなし）
- フィールド名リネーム: `OwnerID` → `PersonInChargeID`、`LentByID` → `CheckedOutByID`、`Type`（lending/self_take）削除

可用性バリデーション本体（AvailablePeriod 対象区域親番制約・期間アクティブ判定）は CheckoutService.Checkout で実装済み（2026-05-07）。
- アクティブ期間が存在しなければ `ErrInvalidState`
- 区域の親番が AvailablePeriod の対象に含まれなければ `ErrPermissionDenied`（編集メンバーもバイパス不可）
- 発行成功時は `Checkout.AvailablePeriodID` に当時のアクティブ期間 ID を記録する

期間終了による強制クローズは `AvailablePeriodService.ForceCloseExpiredCheckouts` として実装済み（バックグラウンド呼び出しタイミングは未配線）。

> **※ 2026-07-13 に AvailablePeriod ごと廃止**（本ブロックは歴史的記録）。上記の期間アクティブ判定・対象区域親番制約・強制クローズはすべて撤廃し、チェックアウトのゲートは排他制約のみに簡素化した（`05_チェックアウト.md` / 上記「SchedulePeriod / AvailablePeriod 廃止」節）。
